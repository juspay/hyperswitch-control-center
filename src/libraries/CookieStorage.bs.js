// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Option from "@rescript/core/src/Core__Option.bs.js";

function getCookieDict() {
  return Object.fromEntries(
    Core__Option.getOr(Caml_option.nullable_to_opt(document.cookie), "")
      .split(";")
      .map(function (value) {
        var arr = value.split("=");
        var key = Core__Option.getOr(arr[0], "").trim();
        var value$1 = Core__Option.getOr(arr[1], "").trim();
        return [key, value$1];
      }),
  );
}

function getCookieVal(key) {
  return Core__Option.getOr(getCookieDict()[key], "");
}

function setCookieVal(cookie) {
  document.cookie = cookie;
}

function getDomainOfCookie(domain) {
  if (domain !== undefined) {
    return domain;
  } else {
    return window.location.hostname;
  }
}

function deleteCookie(cookieName, domain, param) {
  var domainClause =
    "domain=" + (domain !== undefined ? domain : window.location.hostname);
  document.cookie =
    cookieName +
    "=;expires=Thu, 01 Jan 1970 00:00:00 UTC;path=/;" +
    domainClause;
  document.cookie =
    cookieName + "=;expires=Thu, 01 Jan 1970 00:00:00 UTC;path=/;";
}

export {
  getCookieDict,
  getCookieVal,
  setCookieVal,
  getDomainOfCookie,
  deleteCookie,
};
/* No side effect */
