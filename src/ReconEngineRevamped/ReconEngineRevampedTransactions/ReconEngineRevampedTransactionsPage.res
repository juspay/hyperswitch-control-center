@react.component
let make = () => {
  open ReconEngineRevampedHelper

  <PageHeading
    title="Transactions"
    subTitle="Every transaction in range, filter by the account it involves, then open one to trace its entries, rule, history and linked chain."
  />
}
