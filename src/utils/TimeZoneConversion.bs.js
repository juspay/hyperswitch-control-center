// Generated by ReScript, PLEASE EDIT WITH CARE

var timezoneOffset = Object.fromEntries([
  ["IST", "+05:30"],
  ["GMT", "+00:00"],
]);

var timezoneLocation = Object.fromEntries([
  ["IST", "Asia/Kolkata"],
  ["GMT", "UTC"],
]);

function formatter(str) {
  if (str.length === 0) {
    return "00";
  } else if (str.length === 1) {
    return "0" + str;
  } else {
    return str;
  }
}

function convertTimeZone(date, timezoneString) {
  var localTimeString = new Date(date);
  return localTimeString.toLocaleString("en-US", {
    timeZone: timezoneString,
  });
}

function isoStringToCustomTimezone(isoString) {
  var d = timezoneLocation["IST"];
  var timezoneString = d !== undefined ? d : "Asia/Kolkata";
  var timezoneConvertedString = convertTimeZone(isoString, timezoneString);
  var timezoneConverted = new Date(timezoneConvertedString);
  var timeZoneYear = timezoneConverted.getFullYear();
  var timeZoneMonth = timezoneConverted.getMonth();
  var timeZoneDate = timezoneConverted.getDate();
  var timeZoneHour = timezoneConverted.getHours();
  var timeZoneMinute = timezoneConverted.getMinutes();
  var timeZoneSecond = timezoneConverted.getSeconds();
  return {
    year: timeZoneYear,
    month: timeZoneMonth,
    date: timeZoneDate,
    hour: timeZoneHour,
    minute: timeZoneMinute,
    second: timeZoneSecond,
  };
}

export {
  timezoneOffset,
  timezoneLocation,
  formatter,
  convertTimeZone,
  isoStringToCustomTimezone,
};
/* timezoneOffset Not a pure module */
