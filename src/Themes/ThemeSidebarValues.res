open SidebarTypes

let theme = {
  Link({
    name: "Theme",
    link: `/themev2`,
    access: Access,
    icon: "nd-overview",
    selectedIcon: "nd-overview-fill",
  })
}

let themeSidebars = {
  let sidebar = [theme]
  sidebar
}
