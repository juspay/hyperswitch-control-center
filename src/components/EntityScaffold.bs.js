// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as NotFoundPage from "./NotFoundPage.bs.js";
import * as UnauthorizedPage from "./UnauthorizedPage.bs.js";

function EntityScaffold(props) {
  var __renderShow = props.renderShow;
  var __renderNewForm = props.renderNewForm;
  var __access = props.access;
  var remainingPath = props.remainingPath;
  var access = __access !== undefined ? __access : "Access";
  var renderNewForm =
    __renderNewForm !== undefined
      ? __renderNewForm
      : function () {
          return null;
        };
  var renderShow =
    __renderShow !== undefined
      ? __renderShow
      : function (param) {
          return null;
        };
  if (access === "NoAccess") {
    return React.createElement(UnauthorizedPage.make, {});
  }
  if (!remainingPath) {
    return props.renderList();
  }
  var id = remainingPath.hd;
  if (id === "new") {
    if (remainingPath.tl) {
      return React.createElement(NotFoundPage.make, {});
    } else {
      return renderNewForm();
    }
  } else if (remainingPath.tl) {
    return React.createElement(NotFoundPage.make, {});
  } else {
    return renderShow(id);
  }
}

var make = EntityScaffold;

export { make };
/* react Not a pure module */
