// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Core__Option from "@rescript/core/src/Core__Option.bs.js";
import * as LocalStorage from "../libraries/LocalStorage.bs.js";

function getValidToken(oStr) {
  if (oStr !== "__failed" && oStr !== "") {
    return oStr;
  }
}

function useLocalStorageToken(tokenType) {
  var lcToken = getValidToken(LocalStorage.useStorageValue("login"));
  var switchToken = getValidToken(LocalStorage.useStorageValue("switchToken"));
  if (
    Core__Option.isSome(switchToken) &&
    switchToken !== "__failed" &&
    tokenType !== "Original"
  ) {
    return switchToken;
  } else if (
    Core__Option.isSome(lcToken) &&
    lcToken !== "__failed" &&
    tokenType !== "SwitchOnly"
  ) {
    return lcToken;
  } else {
    return;
  }
}

function useTokenParent(tokenType) {
  return useLocalStorageToken(tokenType);
}

export { getValidToken, useLocalStorageToken, useTokenParent };
/* LocalStorage Not a pure module */
