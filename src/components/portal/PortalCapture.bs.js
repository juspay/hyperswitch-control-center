// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Recoil from "recoil";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as PortalState from "./PortalState.bs.js";

var make = React.memo(function (props) {
  var __customStyle = props.customStyle;
  var name = props.name;
  var customStyle = __customStyle !== undefined ? __customStyle : "";
  var setPortalNodes = Recoil.useSetRecoilState(PortalState.portalNodes);
  var setDiv = React.useCallback(
    function (elem) {
      setPortalNodes(function (prevDict) {
        var clonedDict = Object.fromEntries(
          Object.entries(prevDict).filter(function (entry) {
            return entry[0] !== name;
          }),
        );
        if (!(elem == null)) {
          clonedDict[name] = elem;
        }
        return clonedDict;
      });
    },
    [setPortalNodes, name],
  );
  return React.createElement("div", {
    ref: Caml_option.some(setDiv),
    className: customStyle,
  });
});

export { make };
/* make Not a pure module */
