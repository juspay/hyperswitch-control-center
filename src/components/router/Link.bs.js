// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as GlobalVars from "../../utils/GlobalVars.bs.js";
import * as LogicUtils from "../../utils/LogicUtils.bs.js";
import * as RescriptReactRouter from "@rescript/react/src/RescriptReactRouter.bs.js";

function Link(props) {
  var onClick = props.onClick;
  var className = props.className;
  var __openInNewTab = props.openInNewTab;
  var children = props.children;
  var to_ = props.to_;
  var openInNewTab = __openInNewTab !== undefined ? __openInNewTab : false;
  var handleClick = React.useCallback(
    function (ev) {
      ev.stopPropagation();
      ev.preventDefault();
      if (onClick !== undefined) {
        onClick(to_);
      }
      RescriptReactRouter.push(GlobalVars.appendDashboardPath(to_));
    },
    [to_],
  );
  if (openInNewTab) {
    if (LogicUtils.isEmptyString(to_.trim())) {
      return children;
    } else {
      return React.createElement(
        "a",
        {
          className: className,
          href: to_,
          target: "_blank",
          onClick: function (ev) {
            ev.stopPropagation();
          },
        },
        children,
      );
    }
  } else {
    return React.createElement(
      "a",
      {
        className: className,
        href: to_,
        onClick: handleClick,
      },
      children,
    );
  }
}

var make = Link;

export { make };
/* react Not a pure module */
