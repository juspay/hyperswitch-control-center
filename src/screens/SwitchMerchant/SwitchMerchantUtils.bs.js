// Generated by ReScript, PLEASE EDIT WITH CARE

import * as LogicUtils from "../../utils/LogicUtils.bs.js";

function convertListResponseToTypedResponse(json) {
  return LogicUtils.getArrayFromJson(json, []).map(function (ele) {
    var dictOfElement = LogicUtils.getDictFromJsonObject(ele);
    var merchantId = LogicUtils.getString(dictOfElement, "merchant_id", "");
    var merchantName = LogicUtils.isNonEmptyString(
      LogicUtils.getString(dictOfElement, "merchant_name", merchantId),
    )
      ? LogicUtils.getString(dictOfElement, "merchant_name", merchantId)
      : merchantId;
    var role_id = LogicUtils.getString(dictOfElement, "role_id", "");
    var role_name = LogicUtils.getString(dictOfElement, "role_name", "");
    var org_id = LogicUtils.getString(dictOfElement, "org_id", "");
    return {
      merchant_id: merchantId,
      merchant_name: merchantName,
      is_active: LogicUtils.getBool(dictOfElement, "is_active", false),
      role_id: role_id,
      role_name: role_name,
      org_id: org_id,
    };
  });
}

var defaultValue = {
  merchant_id: "",
  merchant_name: "",
  is_active: false,
  role_id: "",
  role_name: "",
  org_id: "",
};

export { defaultValue, convertListResponseToTypedResponse };
/* LogicUtils Not a pure module */
